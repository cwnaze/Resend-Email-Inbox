// Drizzle schema for the Custom Email Inbox database.
//
// This is an intentionally empty baseline for US-A01 (Turso + Drizzle scaffold).
// The full data model (contacts, threads, emails, attachments, auth_codes,
// sessions) is defined in tasks/prd-data-model.md and implemented in US-D01.
// Add table definitions here as that story is worked.

// Empty export keeps this file a module (rather than an ambient script) so
// `import * as schema from './schema'` in db/index.ts type-checks cleanly
// before any tables are defined.
export {};
