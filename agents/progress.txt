## Codebase Patterns
- This SvelteKit toolchain (current `sv create` output, kit ^2.63) configures the Vite plugin and adapter directly in `vite.config.ts` — there is no `svelte.config.js`. Adapter/compiler options go there instead.
- `@sveltejs/adapter-vercel` runtime auto-detection rejects Node versions outside 20/22/24; pass an explicit `runtime` (e.g. `'nodejs22.x'`) to the adapter if the build/dev machine runs a newer Node.
- `.prettierignore` excludes `/tasks/`, `/agents/`, and `/docs/demos/` (PRDs, agent process files, and showboat demo logs) from the app's lint/format scope.
- When writing showboat `exec` blocks meant to be reproducible via `showboat verify`, strip timestamps/durations/PIDs from captured command output (grep/sed) before showboat records it.
- Real secrets (Turso URL/token, etc.) live only in the gitignored root `.env`; `.env.example` tracks variable names with empty values. Never let `showboat exec` capture raw `.env` contents or a connection string into a committed demo doc — pipe drizzle-kit/db output through `grep -v`/`sed` to redact if needed, and grep the demo file for secret-shaped strings before committing.
- Drizzle/Turso: `db` client lives at `src/lib/server/db/index.ts` (server-only import), schema at `src/lib/server/db/schema.ts`, config at root `drizzle.config.ts` (`dialect: 'turso'`). `npm run db:push` applies schema directly (no migration files generated); use `npm run db:generate` if versioned SQL migrations are ever needed.
- R2/Cloudflare object storage is private in this project — no `R2_PUBLIC_URL_BASE`/custom domain. Any story that needs to let the browser fetch/download an R2 object must call `getR2SignedDownloadUrl` (in `src/lib/server/r2/index.ts`) on demand; never assume a stored permanent public URL exists. `tasks/*.md` and `agents/prd.json` wording has been updated repo-wide to say "presigned URL" instead of "public URL" for attachments.
- SvelteKit's `$env/dynamic/private` only resolves inside a Vite/SvelteKit runtime — a standalone `tsx`/`node` script can't `import` app modules that pull in `$env/*`. For one-off infra smoke tests (e.g. `src/lib/server/r2/verify.mts`), read `process.env` directly and run via `node --env-file=.env node_modules/.bin/tsx <script>` instead.
- This repo has no linked Vercel project (no `vercel` CLI, `.vercel/project.json` absent) — any acceptance criterion requiring env vars "in Vercel project settings" can only be verified/set by the project owner; note it as an outstanding manual step rather than blocking `passes: true` on it.
- Tailwind v4 in this project uses CSS-first `@theme` config (no `tailwind.config.js`) inside `src/routes/layout.css`. Dark/light palette theming needs two distinct variable namespaces: raw per-theme values as `--dt-*` custom properties (overridden per theme selector), and `@theme`'s own `--color-*` keys pointing at them via `var(--dt-*)` — naming a `@theme` key the same as its own raw value (e.g. `--color-background: var(--color-background)`) is a self-reference and becomes CSS-invalid.
- `npm run build` actually imports every `+server.ts`/`+page.server.ts` route module during SvelteKit's postbuild analysis step (to detect exported HTTP methods) — a module that throws at import time if an env var is missing (the fail-fast pattern used by `db`, `r2`, and now `email/resend`) will crash `npm run build`, not just `npm run dev`. Any such module needs at least a placeholder value for its required env vars in `.env`, even if the real credential/account isn't provisioned yet in this dev environment.
- For an endpoint that both rate-limits and has an external side effect (DB write, third-party API call), check the rate limit first and return early (429) before doing any write/send — this keeps a rate-limited request from leaving side effects, and keeps the rate-limit path itself deterministic/testable without touching the external API.

- Webhook/signature verification covers the *raw* request bytes: never `request.json()` a signed request before verifying (a `Request` body reads once, and a re-serialized parse won't match). Read text, verify, then parse.
- For any verify-then-act boundary, return a *discriminated* result where the payload exists only on the success branch (`{ ok: true, payload } | { ok: false, reason }`) — that makes "acted on unverified data" a type error rather than a review question.
- Failure detail from an auth/signature check goes to the server log, never the response body: the client gets a bare status. A reason string handed to an unauthenticated caller is a forging aid.
- A provider's webhook payload is a *claim about* an email, not the email. Before writing a parser, fetch one real payload from the provider (their list/get API often lets you do this without waiting for a live delivery) — Resend's `email.received` turned out to carry metadata only, which no amount of reading the PRD would have revealed.
- Status codes at a webhook endpoint encode retry semantics: `200` for verified-but-unusable payloads (nothing to retry), `5xx` only for transient failures (fetch/DB). Inverting them either loses mail silently or creates an infinite redelivery loop.
- For an idempotent upsert under concurrent redelivery, `insert(...).onConflictDoNothing().returning()` + a re-read on the empty result is the pattern — a bare insert turns a lost race into a 500, which the provider then retries forever.
- Normalize the case of any value covered by a SQLite unique index at the write boundary (SQLite's `unique` is case-sensitive); compare with `lower()` on reads so rows written before the normalization existed still match.
- A showboat `exec` block that needs a running dev server plus a secret should be self-contained — export a throwaway secret inline, start the server with it in the environment, `trap`-kill on exit — so `showboat verify` doesn't depend on the gitignored `.env`.

## Progress Log

## [2026-07-22] - US-A00
- Scaffolded a fresh SvelteKit project (TypeScript, Svelte 5 with runes forced) at the repo root using the official `sv create` CLI with add-ons: prettier, eslint, tailwindcss (v4, via @tailwindcss/vite), sveltekit-adapter (adapter: vercel). Merged the generated project into the existing repo root (which previously only had tasks/ and agents/), keeping tasks/ and agents/ untouched.
- Set an explicit Vercel adapter runtime (`nodejs22.x`) in vite.config.ts since the dev/build machine runs Node v26, which adapter-vercel's runtime auto-detection rejects.
- Excluded /tasks/, /agents/, and /docs/demos/ from .prettierignore so pre-existing PRD/agent files and showboat demo logs don't fail `npm run lint`.
- Added root CLAUDE.md documenting the stack, the vite.config.ts-instead-of-svelte.config.js quirk, and the adapter runtime gotcha.
- Verified: `npm run check` (0 errors), `npm run lint` (prettier + eslint clean), `npm run build` (adapter-vercel build succeeds), `npm run dev` serves the default placeholder page, confirmed live via rodney (page title/h1 "Welcome to SvelteKit", screenshot captured). Full proof-of-work recorded in docs/demos/US-A00.md (showboat verify passes, exit 0).
- Files changed: package.json, package-lock.json, vite.config.ts, tsconfig.json, eslint.config.js, prettier.config.js, .gitignore, .prettierignore, .npmrc, .vscode/, README.md, static/, src/** (app.html, app.d.ts, lib/, routes/+layout.svelte, routes/layout.css, routes/+page.svelte), CLAUDE.md (new), docs/demos/US-A00.md (new), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - No svelte.config.js exists in this project — adapter and Svelte compiler options are configured inside vite.config.ts via the sveltekit() plugin call. Don't go looking for svelte.config.js.
  - If a build fails locally with "Unsupported Node.js version", it's the adapter-vercel runtime check, not an app bug — the explicit runtime override is already in place in vite.config.ts.
  - Tailwind v4 has no tailwind.config.js by default; it's wired purely through `@tailwindcss/vite` in vite.config.ts plus `@import 'tailwindcss';` in src/routes/layout.css, imported from +layout.svelte.
---

## [2026-07-22] - US-A01
- Installed `drizzle-orm`, `@libsql/client` (dependencies) and `drizzle-kit` (devDependency).
- Added `src/lib/server/db/schema.ts`: intentionally empty baseline schema module (just `export {}` plus comments pointing to tasks/prd-data-model.md and US-D01, which will define the real tables). Kept deliberately empty per this story's scope — full schema is a separate story.
- Added `src/lib/server/db/index.ts`: exports `db`, a `drizzle-orm/libsql` client built from `createClient({ url, authToken })`, reading `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` via `$env/dynamic/private` and throwing early if the URL is missing.
- Added root `drizzle.config.ts` (`dialect: 'turso'`, `schema: './src/lib/server/db/schema.ts'`, `out: './drizzle'`), reading the same two env vars from `process.env` (drizzle-kit's bundled dotenv auto-loads the repo's `.env`).
- Added `npm run db:push` / `db:generate` / `db:studio` scripts wrapping `drizzle-kit push|generate|studio`.
- Added `.env.example` documenting `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` with empty values (real values only in the gitignored `.env`, already provisioned by the project owner before this story started).
- Verified: `npm run db:push -- --force` ran against the real Turso database already configured in `.env` and reported "No changes detected" (correct for an empty baseline schema against a fresh DB). `npm run check` (0 errors), `npm run lint` (prettier + eslint clean), `npm run build` (adapter-vercel build still succeeds). Full proof recorded in docs/demos/US-A01.md (showboat verify passes, exit 0); no rodney/browser step since this story has no UI surface.
- Files changed: package.json, package-lock.json, drizzle.config.ts (new), .env.example (new), src/lib/server/db/index.ts (new), src/lib/server/db/schema.ts (new), CLAUDE.md, docs/demos/US-A01.md (new), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - `schema.ts` must export *something* (even just `export {}`) or TS/svelte-check errors with "is not a module" when other files do `import * as schema from './schema'` — relevant again once US-D01 replaces the placeholder with real tables (the export shape will change naturally once tables exist).
  - drizzle-kit 0.31.x bundles its own dotenv loading, so `drizzle.config.ts` can read `process.env.TURSO_*` directly without an explicit `import 'dotenv/config'` — no extra dependency needed.
  - `npm run db:push` needs `-- --force` (i.e. `npm run db:push -- --force`) to skip the interactive confirmation prompt in a non-interactive shell; safe here since the baseline schema has no destructive changes to confirm.
---

## [2026-07-22] - US-A02
- Installed `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner` (dependencies).
- Added `src/lib/server/r2/index.ts`: three server-only utilities — `uploadToR2(key, body, contentType?)`, `deleteFromR2(key)`, `getR2SignedDownloadUrl(key, expiresInSeconds?)` (default 15 min) — built on the S3-compatible API at `https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com`. Validates all four required env vars (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`) at import time.
- **Architecture decision, per project owner:** the R2 bucket is private (no public bucket URL/custom domain), so there is no `R2_PUBLIC_URL_BASE` env var. Updated `agents/prd.json` (US-A02, US-E05, US-G03 acceptance criteria) and `tasks/prd-architecture.md`, `tasks/prd-data-model.md`, `tasks/prd-feature-inbound-processing.md`, `tasks/prd-feature-thread-view.md` to drop `R2_PUBLIC_URL_BASE`/`attachments.r2_public_url` and describe presigned-URL-on-demand access instead. Future stories (US-D01 attachments table, US-E05 ingestion, US-G03 download) should follow this: store only `r2_object_key`, generate presigned URLs at render/request time.
- Added `.env.example` entries for the four R2 vars (empty values); real values already populated in the gitignored `.env` by the project owner before this story started.
- Added `src/lib/server/r2/verify.mts`: a standalone smoke-test script (run via `node --env-file=.env node_modules/.bin/tsx src/lib/server/r2/verify.mts`) that exercises the same S3-compatible calls end-to-end against the real bucket — upload, presign+fetch (200, body match), delete, then confirms the presigned URL 404s post-delete. Kept as a committed dev utility (not a throwaway file) since `$env/dynamic/private` can't be imported outside a Vite/SvelteKit runtime, so this is the practical way to re-verify R2 connectivity standalone.
- Verified: `src/lib/server/r2/verify.mts` full round-trip passes against the real R2 bucket. `npm run check` (0 errors), `npm run lint` (prettier + eslint clean), `npm run build` (adapter-vercel build succeeds). Full proof recorded in docs/demos/US-A02.md (showboat verify passes, exit 0); no rodney/browser step since this story has no UI surface.
- **Outstanding manual step (not blocking):** this environment has no linked Vercel project (no `vercel` CLI, no `.vercel/project.json`), so the four R2 env vars still need to be added to the Vercel project's settings by the project owner before deploying — same as they were already added locally in `.env`.
- Files changed: package.json, package-lock.json, .env.example, CLAUDE.md, src/lib/server/r2/index.ts (new), src/lib/server/r2/verify.mts (new), docs/demos/US-A02.md (new), agents/prd.json, agents/progress.txt, tasks/prd-architecture.md, tasks/prd-data-model.md, tasks/prd-feature-inbound-processing.md, tasks/prd-feature-thread-view.md.
- **Learnings for future iterations:**
  - The bucket is private — always generate presigned URLs on demand via `getR2SignedDownloadUrl`; never store a permanent public URL for an attachment.
  - `$env/dynamic/private` (and any `$env/*` import) only resolves inside SvelteKit/Vite. Standalone infra scripts run via plain `tsx`/`node` must read `process.env` directly and be invoked with `node --env-file=.env ...` (or similar) instead of importing app modules that pull in `$env/*`.
  - No Vercel project is linked in this dev environment — treat "available... in Vercel project settings" acceptance criteria as a manual step for the project owner to complete separately, not something verifiable here.
---

## [2026-07-23] - US-A03
- Added `src/lib/server/auth/session.ts`: exports `SESSION_COOKIE_NAME` (`'session'`) and `hasSessionCookie(cookies)`, a presence/non-empty check only — no DB validation yet since the `sessions` table doesn't exist until US-B01.
- Added `src/routes/(app)/+layout.server.ts`: calls `hasSessionCookie`, redirects (302) to `/login` if absent. Added `src/routes/(app)/+layout.svelte` (pass-through `{@render children()}`).
- Added `src/routes/(app)/inbox/+page.svelte` (placeholder "Inbox" page, exists purely so `/inbox` has a protected route to verify redirect behavior against) and `src/routes/login/+page.svelte` (placeholder "Login" page).
- Added `.rodney` to `.gitignore` — `rodney --local start` creates local browser-automation state under `.rodney/` that isn't gitignored anywhere yet in this project; without the ignore entry it shows up as untracked files and trips `prettier --check .` in `npm run lint`.
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (adapter-vercel build succeeds). Ran `npm run dev` and confirmed via `curl`: unauthenticated `GET /inbox` → `302` to `/login`; same request with a `session=<any-non-empty-value>` cookie → `200`. Confirmed via rodney (`rodney --local open http://localhost:5173/inbox` → final `url`/`title` both `/login`), screenshot captured. Full proof recorded in docs/demos/US-A03.md (showboat verify passes, exit 0).
- Files changed: src/lib/server/auth/session.ts (new), src/routes/(app)/+layout.server.ts (new), src/routes/(app)/+layout.svelte (new), src/routes/(app)/inbox/+page.svelte (new), src/routes/login/+page.svelte (new), .gitignore, CLAUDE.md, docs/demos/US-A03.md (new), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - Route protection for all mailbox pages is centralized in `src/routes/(app)/+layout.server.ts` — new protected pages just need to live under `(app)/`, no per-route auth code needed.
  - The session check is presence-only for now (no `sessions` table until US-B01). When implementing US-B01/US-B05, replace the check inside `(app)/+layout.server.ts` with a real hash-and-lookup against `sessions`, rather than adding a second parallel check elsewhere.
  - `rodney --local start` writes local state to `.rodney/` (Chrome profile data, etc.) — this needs a `.gitignore` entry (now added) or it silently fails `npm run lint`'s `prettier --check .` step on unrelated stories that happen to run rodney first.
---

## [2026-07-23] - US-D01
- Replaced the empty baseline `src/lib/server/db/schema.ts` with the full data model from `tasks/prd-data-model.md`: `contacts`, `threads`, `emails`, `attachments`, `auth_codes`, `sessions`. Booleans use `integer({ mode: 'boolean' })`, address-list columns (`to_emails`/`cc_emails`/`bcc_emails`) use `text({ mode: 'json' })` typed as `string[]`, timestamps use `integer({ mode: 'timestamp_ms' })` with a `default(sql\`(unixepoch('subsec') * 1000)\`)` for the various `created_at`/`updated_at` columns.
- Declared all 8 indexes specified in the PRD via the `(table) => [...]` index-builder array form (current drizzle-orm/drizzle-kit API — the old second-arg-object form is deprecated): 3 unique (`contacts.email`, `emails.message_id`, `sessions.token_hash`) and 5 non-unique (`emails.thread_id`, `emails.is_read`+`is_deleted` composite, `threads.last_message_at`, `attachments.email_id`, `auth_codes.expires_at`).
- Declared FKs via `.references(() => table.column)`: `emails.thread_id -> threads.id`, `attachments.email_id -> emails.id`.
- Applied via `npm run db:push -- --force` against the real Turso database (already provisioned since US-A01) — first run created all 6 tables/8 indexes; immediate re-run reported "No changes detected", confirming the push is idempotent.
- Added `src/lib/server/db/verify-schema.mts` (standalone `tsx` smoke test, same pattern as `src/lib/server/r2/verify.mts` from US-A02 — reads `process.env` directly since `$env/dynamic/private` can't be imported outside a SvelteKit/Vite runtime): confirms all 6 tables and all 8 indexes exist via `sqlite_master` introspection, then does live INSERT tests confirming the `emails.thread_id` FK rejects an orphan reference and the `emails.message_id` UNIQUE index rejects a duplicate — both inside a `PRAGMA foreign_keys = ON` session (libSQL, like SQLite, doesn't enforce FKs by default per-connection). Cleans up its own test rows after running.
- Also added a `.rodney` entry to `.gitignore` (untracked rodney browser-automation state was failing `npm run lint`'s `prettier --check .` step in this environment) — unrelated to the schema work itself but needed to get quality checks green.
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (adapter-vercel build succeeds), `verify-schema.mts` all 4 assertions pass against the live Turso DB. No rodney/browser step — this story has no UI surface. Full proof recorded in docs/demos/US-D01.md (showboat verify passes, exit 0).
- Files changed: src/lib/server/db/schema.ts, src/lib/server/db/verify-schema.mts (new), .gitignore, docs/demos/US-D01.md (new), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - Drizzle's current index-declaration API for `sqliteTable` is the third-argument callback returning an array (`(table) => [index(...), uniqueIndex(...)]`), not an object keyed by arbitrary property names — use the array form for any new tables/indexes.
  - libSQL/SQLite does not enforce foreign keys by default per-connection; any script or app code relying on FK constraint errors (e.g. to validate referential integrity) must `PRAGMA foreign_keys = ON` first on that connection.
  - `src/lib/server/db/verify-schema.mts` is the reusable pattern for verifying schema changes against the live Turso DB going forward (mirrors `src/lib/server/r2/verify.mts`) — future schema-changing stories can extend or copy this rather than writing ad hoc one-off scripts outside the repo.
---

## [2026-07-23] - US-J01
- Added the Dusk Terminal design tokens (tasks/prd-ui-ux.md) as Tailwind v4 CSS-first `@theme` config in `src/routes/layout.css` (this project has no `tailwind.config.js`).
- Palette: dark values in `:root` (reference theme), light values under `@media (prefers-color-scheme: light) { :root:not([data-theme]) {...} }` (OS-default fallback), and again under `:root[data-theme='dark']`/`:root[data-theme='light']` for an explicit user choice that always wins. Raw values are `--dt-*` custom properties; `@theme` maps Tailwind's `--color-*` namespace onto them (`--color-background: var(--dt-background)`, etc.) so `bg-background`, `text-text-primary`, `border-border`, `text-accent`, `text-accent-secondary`, `text-danger` utilities exist and theme-switch automatically.
- Fonts: installed `@fontsource/inter` and `@fontsource/jetbrains-mono` (self-hosted, no CDN calls at runtime), imported only the weights used (Inter 400/500/600/700, JetBrains Mono 400/500) in `layout.css`, exposed as `font-sans`/`font-mono` via `@theme`.
- Radius: overrode the entire Tailwind radius namespace (`--radius-xs` through `--radius-full`, plus `DEFAULT`) to values in the 2-4px range, so `rounded-full` is not pill-shaped in this app — matches the "no pill shapes" rule.
- Motion: added `--duration-fast` (120ms) / `--duration` (140ms) / `--duration-slow` (160ms) and `--ease-standard` (`cubic-bezier(0, 0, 0.2, 1)`) theme vars, giving matching Tailwind utilities for all transitions.
- Added `src/lib/theme.svelte.ts` (a `$state`-backed `themeState` plus `initTheme`/`applyTheme`/`toggleTheme`) that sets/clears the `data-theme` attribute on `<html>` and persists the explicit choice to `localStorage` (`dusk-terminal-theme` key); `src/routes/+layout.svelte` calls `initTheme()` on mount. Added a small inline pre-hydration script in `src/app.html` that applies any stored theme before first paint (avoids a flash of the wrong theme).
- Added `src/lib/components/ThemeToggle.svelte` (reusable toggle button) and `src/routes/dev/tokens/+page.svelte` (unauthenticated dev-only page, outside the `(app)` route group) rendering palette swatches, font samples, radius samples, and a motion demo, with the toggle wired in — used purely for visual QA of the tokens, not part of the real app shell (US-J02 builds that).
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (adapter-vercel build succeeds). Verified live via rodney against `/dev/tokens`: default load is dark (`color-scheme: dark`, body background `rgb(18, 20, 28)` = `#12141C`), fonts resolve to `Inter`/`JetBrains Mono`, radius utilities compute to `2px`/`3px` as expected; clicking the toggle switches `data-theme` to `light`, persists to `localStorage`, and body background updates to `rgb(245, 244, 240)` = `#F5F4F0`. Screenshots of both themes captured. Full proof recorded in docs/demos/US-J01.md (showboat verify passes, exit 0).
- Files changed: package.json, package-lock.json, src/app.html, src/routes/+layout.svelte, src/routes/layout.css, src/lib/theme.svelte.ts (new), src/lib/components/ThemeToggle.svelte (new), src/routes/dev/tokens/+page.svelte (new), docs/demos/US-J01.md (new, + 2 screenshot assets), CLAUDE.md, agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - Tailwind v4's CSS-first `@theme` requires distinct variable names between the raw per-theme value and the `@theme` key that exposes it as a utility — `--color-background: var(--color-background)` is a self-reference and computes to invalid, silently breaking the utility. Use a separate raw namespace (this project uses `--dt-*`) and point `@theme`'s `--color-*` keys at those.
  - `rodney --local js "<expr>"` evaluates as a single expression, not a statement sequence — a `foo; bar; 'baz'` snippet throws `Unexpected token ';'`; wrap multi-statement snippets in an IIFE: `(function(){ ...; return 'done'; })()`.
  - Future stories needing new design tokens (e.g. spacing, shadows if ever added) should extend the `@theme` block in `src/routes/layout.css` and the `/dev/tokens` page, not create parallel token files.
---

## [2026-07-23] - US-J02
- Replaced the pass-through `src/routes/(app)/+layout.svelte` with the real app shell: a top bar (`h-14`, app mark linking to `/inbox` via `resolve('/inbox')`, a `role="search"` input as the search entry point, and a `POST /logout` form/button) plus a two-pane body — a fixed `w-[360px]` left "list" column (`hidden lg:flex`, currently just a placeholder string; the real thread list replaces it in US-F01) and a flexible right pane rendering `{@render children()}`. Below Tailwind's `lg` breakpoint (1024px) the left column is hidden entirely, giving the single-pane stack the design calls for, with `min-w-0` on the flex containers so nothing overflows horizontally down to 375px.
- Added `src/routes/logout/+server.ts`: a `POST` handler that calls `cookies.delete(SESSION_COOKIE_NAME, { path: '/' })` and redirects (303) to `/login`. Real session-row invalidation (deleting from the `sessions` table) lands in US-B05 once that table is actually read from for auth; for now this is consistent with `hasSessionCookie`'s presence-only check from US-A03.
- Verified: `npm run check` (0 errors), `npm run lint` (clean — the internal `<a href>` link needed `resolve('/inbox')` from `$app/paths` instead of a raw string, per the `svelte/no-navigation-without-resolve` lint rule enforced in this kit version), `npm run build` (adapter-vercel build succeeds). Verified live via rodney with a demo session cookie set through `document.cookie` (real login/session issuance isn't built until US-B02/B03): at 1280px the two-pane shell renders (left column + top bar + right pane content), at 375px the left column is hidden and only a single-pane stack renders with no horizontal overflow, and clicking "Log out" redirects to `/login`. curl also reconfirmed route protection is unaffected (unauth `/inbox` -> 302 to `/login`; with a cookie -> 200). Screenshots and full proof recorded in docs/demos/US-J02.md (showboat verify passes, exit 0).
- Files changed: src/routes/(app)/+layout.svelte, src/routes/logout/+server.ts (new), docs/demos/US-J02.md (new, + 2 screenshot assets), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - This kit version enforces `svelte/no-navigation-without-resolve` — any internal `<a href="/...">` (or programmatic navigation) must wrap the path in `resolve(...)` from `$app/paths` (e.g. `href={resolve('/inbox')}`), not a raw string literal, or `npm run lint` fails.
  - `rodney screenshot -w <W> -h <H>` resizes the emulated viewport for that single capture (valid evidence for responsive/viewport acceptance criteria) but the viewport reverts afterward — a `rodney js` call issued after the screenshot still reports the previous (e.g. 1280) `window.innerWidth`. Don't chain a post-screenshot `rodney js` assertion expecting the narrower viewport to still be active; treat the screenshot image itself as the proof for viewport-dependent layout checks.
  - The left "list" column added in this story is intentionally a structural placeholder (plain text, not the `EmptyState` component) — US-J03 (shared empty/loading/error components) hasn't landed yet. Once it does, and once US-F01 adds the real thread list, replace the placeholder `<p>` inside the `<aside>` rather than restructuring the shell around it.
---

## [2026-07-27] - US-B01
- Added `src/lib/server/auth/auth-codes.ts`: Drizzle query helpers for the `auth_codes` table — `createAuthCode`, `getActiveAuthCode` (unused + unexpired, latest-first), `invalidateActiveAuthCodes` (supersedes all active codes, for "only one active code at a time" when a new one is requested), `incrementAuthCodeAttempts`, `markAuthCodeUsed`.
- Added `src/lib/server/auth/sessions-store.ts`: Drizzle query helpers for the `sessions` table — `createSession`, `getValidSessionByTokenHash` (unexpired only), `extendSessionExpiry` (sliding expiration per the Data Model PRD), `deleteSessionByTokenHash`. Named `sessions-store.ts` (not `sessions.ts`) to avoid clashing with the existing `session.ts` (cookie-name/presence-check helper from US-A03, untouched by this story).
- Both modules take a `db` handle as their first argument, typed as `Database` from `src/lib/server/db/types.ts` (`import type { db } from './index'; export type Database = typeof db`) rather than importing the `db` singleton directly — `import type` only pulls the type, not the module's runtime code, so this incurs no `$env/dynamic/private` dependency. This lets the exact same query functions run from real app code (which will pass the actual `db` singleton, in US-B02/B03/B05) and from a standalone verification script (which builds its own `drizzle(createClient(...))` instance from `process.env`).
- Added `src/lib/server/auth/verify-auth-sessions.mts`: standalone `tsx` smoke test (same run pattern as `src/lib/server/db/verify-schema.mts` and `src/lib/server/r2/verify.mts`) exercising all of the above against the live Turso database — 19 assertions covering create/read/supersede/expire/use/attempt-increment (including a concurrent-increment and a double-use compare-and-swap check) for auth_codes and create/read/extend/delete for sessions, all pass. Body is wrapped in `try`/`finally` so inserted rows are cleaned up and the libSQL client closed even if an assertion throws — it writes to the same live Turso instance the app uses, so leaving debris behind is not acceptable.
- PR review (#11) follow-up, addressing three correctness issues before US-B02/B03/B05 build on these helpers: (1) `incrementAuthCodeAttempts` now increments in SQL rather than doing a JS read-modify-write, so concurrent wrong-code submissions can't lose updates and walk past the 5-attempt lockout; (2) `markAuthCodeUsed` gained an `isNull(usedAt)` guard, making code consumption an atomic compare-and-swap whose `undefined` return means "already consumed, reject"; (3) `invalidateActiveAuthCodes` now supersedes by pulling `expires_at` back to now instead of stamping `used_at`, keeping "redeemed" distinguishable from "superseded" in the data. Also extracted the duplicated `Database` type to `src/lib/server/db/types.ts` and added an explicit `.limit(1)` to `getValidSessionByTokenHash`.
- Verified: `verify-auth-sessions.mts` (19/19 PASS), `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (adapter-vercel build succeeds). No rodney/browser step — this story has no UI surface. Full proof recorded in docs/demos/US-B01.md (showboat verify passes, exit 0).
- Files changed: src/lib/server/auth/auth-codes.ts (new), src/lib/server/auth/sessions-store.ts (new), src/lib/server/auth/verify-auth-sessions.mts (new), src/lib/server/db/types.ts (new), docs/demos/US-B01.md (new), CLAUDE.md, agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - Use `createAuthCode`/`getActiveAuthCode`/`invalidateActiveAuthCodes`/`incrementAuthCodeAttempts`/`markAuthCodeUsed` from `src/lib/server/auth/auth-codes.ts` and `createSession`/`getValidSessionByTokenHash`/`extendSessionExpiry`/`deleteSessionByTokenHash` from `src/lib/server/auth/sessions-store.ts` in US-B02 (request-code endpoint), US-B03 (verify-code endpoint), and US-B05 (route protection/logout) — don't write new raw Drizzle queries against these tables, extend these modules instead.
  - The dependency-injected-db-handle pattern (import the shared `Database` type from `src/lib/server/db/types.ts`, then take `database: Database` as the first function argument instead of importing the `db` singleton at module scope) is a general, reusable pattern for any future server module that needs to be both (a) called from real SvelteKit server code and (b) exercised by a standalone `tsx` verification script outside the Vite runtime. Prefer it over duplicating query logic between app code and a verify script.
  - `invalidateActiveAuthCodes` implements "only one active code at a time" by pulling `expires_at` back to now on any still-active code when a new one is requested — US-B02 must call this (or equivalent) before/when inserting a new `auth_codes` row. It deliberately does NOT set `used_at`, so `used_at IS NOT NULL` keeps meaning "actually redeemed": US-B03's error copy for a superseded code should say expired / no-longer-valid, not "already used".
  - `markAuthCodeUsed` is an atomic compare-and-swap (`used_at IS NULL` guard). US-B03 must treat an `undefined` return as "another request already consumed this code" and refuse to mint a session — a read-then-check-then-mark sequence without honoring that return value reintroduces the double-session race.
  - `extendSessionExpiry` exists for the "sliding expiration, updated on activity" note in `tasks/prd-data-model.md` on `sessions.expires_at` — US-B05 (or wherever session activity is tracked) should call it rather than leaving sessions at a fixed expiry.
---

## [2026-07-27] - US-B02
- Note: this iteration branched off `ralph/us-b01-auth-sessions-wiring` (not `main`) since US-B02 depends on US-B01's `auth-codes.ts`/`sessions-store.ts` helpers and US-B01's PR (#11) wasn't merged to `main` yet at the time this story started. US-J03's PR (#10) was also already open/complete when this iteration began, so it was left alone rather than re-implemented.
- Added `countAuthCodeRequestsSince(database, windowStart)` to `src/lib/server/auth/auth-codes.ts`: counts `auth_codes` rows created at/after a given time, regardless of used/expired state — this is the "requests in the rolling window" count for rate limiting, not the "active code" count.
- Added `src/lib/server/email/resend.ts`: thin wrapper around the `resend` npm package. Exports `AUTH_RECIPIENT_EMAIL` (read from the env var of the same name at import time — never from request input, per FR-1) and `sendAuthCodeEmail(code)` (sends from `auth@caseynazelrod.com`). Throws at import time if `RESEND_API_KEY`/`AUTH_RECIPIENT_EMAIL` are missing, same fail-fast pattern as `db`/`r2`.
- Added `src/routes/api/auth/request-code/+server.ts` (`POST`): checks `countAuthCodeRequestsSince` against a 10-minute rolling window first and returns `429` immediately if >= 3 (before any DB write or Resend call); otherwise invalidates any active code, generates a CSPRNG 6-digit code (`node:crypto randomInt`), stores its SHA-256 hash via `createAuthCode` with `expires_at = now + 10 min`, and calls `sendAuthCodeEmail`.
- Added `RESEND_API_KEY`/`AUTH_RECIPIENT_EMAIL` to `.env.example` (empty). Discovered mid-story that `npm run build` imports every `+server.ts` at postbuild-analysis time, so these two vars need at least a placeholder value in the local `.env` for the build not to crash — added placeholders locally (`RESEND_API_KEY=re_placeholder_not_a_real_key`, real `AUTH_RECIPIENT_EMAIL=cwnaze@gmail.com`) since no real Resend account is provisioned in this dev environment.
- Added `src/lib/server/auth/verify-request-code.mts`: standalone `tsx` smoke test (same pattern as other `verify-*.mts` scripts) covering code generation/hashing, rate-limit counting, and invalidation, run directly against the live Turso DB; cleans up its own rows by id afterward (11/11 assertions pass, reproducible on repeat runs).
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (succeeds with placeholder Resend env vars present). `verify-request-code.mts` 11/11 PASS. Manual live curl checks against a locally running dev server: (a) seeding 3 `auth_codes` rows then POSTing returned `429` with no new row/email send; (b) POSTing with fewer rows present created a row and reached the real Resend API, which rejected the placeholder API key with a clean `401 API key is invalid` (confirms correct wiring; a real key is needed for actual delivery). All manually-seeded/created test rows were deleted by id afterward. No rodney/browser step — this story's acceptance criteria don't require one (UI lands in US-B04). Full proof recorded in docs/demos/US-B02.md (showboat verify passes, exit 0).
- Files changed: package.json, package-lock.json, .env.example, .env (local only, gitignored — placeholder Resend vars), src/lib/server/auth/auth-codes.ts, src/lib/server/email/resend.ts (new), src/routes/api/auth/request-code/+server.ts (new), src/lib/server/auth/verify-request-code.mts (new), CLAUDE.md, docs/demos/US-B02.md (new), agents/prd.json, agents/progress.txt.
- PR review (#12) follow-up: (1) a failed Resend send now rolls back the just-created `auth_codes` row via a new `deleteAuthCode` helper and returns a generic `502` — previously a transient Resend failure invalidated the user's working code, burned a rate-limit slot, and committed a row whose plaintext was lost forever, so three blips locked them out for 10 minutes; (2) `resend.ts` no longer echoes the provider's error message into the thrown `Error` (logs it server-side, throws generic copy); (3) both Resend env reads are now lazy (memoized client + `getAuthRecipient()`) instead of import-time, so `npm run build` — which imports every `+server.ts` — no longer requires real or placeholder secrets (verified: build succeeds with both vars commented out of `.env`); (4) the duplicated `createHash('sha256')` was extracted to a single exported `hashAuthCode` in `auth-codes.ts`, with a comment recording that plain SHA-256 over a 10^6 preimage space is a deliberate choice; (5) the `429` now carries a `Retry-After` header; (6) merged the single `node:crypto` import, and fixed two bad assertions in `verify-request-code.mts` (a tautological `codeA !== codeB || codeA === codeB`, and a `before === 0` baseline that would flake against the shared live DB if any code was legitimately requested in the preceding 10 minutes — both counts are now scoped to rows this run creates).
- Also merged `ralph/us-b01-auth-sessions-wiring` in, so this branch carries US-B01's own post-review helpers (SQL-side attempt increment, compare-and-swap `markAuthCodeUsed`, supersede-by-`expires_at`) rather than the pre-review versions it originally branched from. `verify-request-code.mts`'s invalidation assertions were updated accordingly: superseding pulls `expires_at` back and must never set `used_at`.
- **Outstanding manual step (not blocking):** a real Resend API key (and `AUTH_RECIPIENT_EMAIL`) must be provisioned in `.env` and Vercel project settings by the project owner before this endpoint can actually deliver emails in production — same pattern as R2/Turso credentials in earlier stories.
- **Learnings for future iterations:**
  - `npm run build`'s postbuild analysis step actually *imports* every route module (to detect which HTTP methods it exports) — a fail-fast `requireEnv`-style check at module scope (like `db`, `r2`, and now `email/resend`) will crash the build itself if that env var is unset, not just `npm run dev`. New server modules with this pattern need at least a placeholder value in `.env` before `npm run build` will succeed, even before the real credential exists.
  - For rate-limited endpoints with external side effects (DB writes, third-party API calls), check the rate limit and return early *before* any write/send — keeps the 429 path side-effect-free and independently testable without needing the external service to succeed.
  - When a story's dependency (US-B01) hasn't merged to `main` yet, branch off that dependency's still-open branch instead of `main` and note it in progress.txt, rather than re-deriving/duplicating its helpers from scratch or waiting.
---

## [2026-07-27] - US-B03
- Note: branched off `ralph/us-b02-request-code-endpoint` (not merged to `main` yet) since this story depends on that branch's `auth-codes.ts`/`sessions-store.ts`/`email/resend.ts` helpers, same reasoning US-B02 used for branching off US-B01's branch. US-J03 (priority 8, lower than US-B03's 11) was skipped for this iteration because it already had a completed implementation and an open, unmerged PR (#10) from a prior iteration — redoing it would have been duplicate/conflicting work; US-B01 (#11) and US-B02 (#12) are in the same state. US-B03 was the lowest-priority story with no branch/PR already in flight.
- Added `getLatestAuthCode(database)` to `src/lib/server/auth/auth-codes.ts`: returns the single most-recently-created `auth_codes` row regardless of used/expired state. This is deliberately different from US-B01's `getActiveAuthCode` (which only returns unused+unexpired rows) — verify-code needs to distinguish "no code ever requested", "code exists but already used", and "code exists but expired" so it can return the right error message *and* skip incrementing `attempt_count` against a dead code, which `getActiveAuthCode` alone can't express (it just returns null for all three "not currently active" cases).
- Added `src/routes/api/auth/verify-code/+server.ts` (`POST`): validates the body is a 6-digit string, fetches `getLatestAuthCode`, and branches: no row -> 400 "no code requested"; `usedAt !== null` -> 400 "already used" (no attempt increment); expired -> 400 "expired" (no attempt increment); otherwise SHA-256-hashes the submitted code and compares via `timingSafeEqual` (constant-time, not `===`) against the stored hash. On match: `markAuthCodeUsed`, generate an opaque 32-byte session token (`randomBytes(32).toString('hex')`), hash it (same SHA-256 helper), `createSession` with a 30-day expiry (FR-9), and `cookies.set('session', <raw token>, { httpOnly: true, secure: true, sameSite: 'lax', expires })`. On mismatch: `incrementAuthCodeAttempts`; if the resulting `attemptCount` reaches 5, also `markAuthCodeUsed` (invalidates the code) and returns a distinct "too many attempts" message instead of the generic "incorrect code" one.
- Added `src/lib/server/auth/verify-verify-code.mts` (same standalone `tsx`-run pattern as the other `verify-*.mts` scripts in this repo): 20 assertions against the live Turso DB covering correct-code match + session creation + single-use rejection, incorrect-code attempt counting through the 5th-attempt invalidation, and expired-code rejection without incrementing `attempt_count`. All rows/sessions it creates are cleaned up by id before exit.
- Verified: `verify-verify-code.mts` (20/20 PASS), `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (succeeds). Manual live curl checks against a running dev server with a directly-seeded code row: incorrect code -> `400`, no cookie; correct code -> `200` with `set-cookie: session=<64-hex>; Path=/; Expires=...; HttpOnly; Secure; SameSite=Lax`; re-submitting the same correct code afterward -> `400 "already been used"`. No rodney/browser step — this story has no UI surface (US-B04 adds that). Full proof recorded in `docs/demos/US-B03.md` (showboat verify passes, exit 0).
- Files changed: src/lib/server/auth/auth-codes.ts, src/routes/api/auth/verify-code/+server.ts (new), src/lib/server/auth/verify-verify-code.mts (new), docs/demos/US-B03.md (new), CLAUDE.md, agents/prd.json, agents/progress.txt.
- Merged `main` in after US-B01 (#11) and US-B02 (#12) landed, and reconciled this story with their post-review semantics, which this branch predated:
  - The 5-attempt lockout no longer calls `markAuthCodeUsed`. It calls a new `expireAuthCode(database, id, now)` helper (pulls `expires_at` back for one row — the single-row form of `invalidateActiveAuthCodes`). A code burned by wrong guesses was never redeemed, so stamping `used_at` both destroyed `used_at`'s meaning as the "was this ever redeemed?" audit answer and made verify-code's own already-used branch tell the user their code was "already used" when they never entered it correctly. Confirmed live: after five wrong guesses, submitting the correct code now returns "This code has expired", and the row's `used_at` is still `null`.
  - The success path now honors `markAuthCodeUsed`'s compare-and-swap return value: `undefined` (someone else already consumed this code) returns the already-used error instead of falling through to mint a second session. Ignoring it was exactly the double-session race US-B01's guard was added to make detectable.
  - Replaced all four inlined `createHash('sha256')` copies with the shared `hashAuthCode` from `auth-codes.ts` (US-B02). The session *token* hash got its own `hashSessionToken` in `sessions-store.ts` rather than reusing `hashAuthCode` — identical body, but the two hash different secrets with different threat models (a 32-byte random token has no brute-forceable preimage space; a 6-digit code does), so keying one later must not silently key the other.
- **Learnings for future iterations:**
  - When a helper's semantics are decided in one story's PR review, downstream branches that were cut *before* that review need an explicit reconciliation pass — merging `main` in resolves the textual conflict but not the semantic one. Here the merge left the lockout calling `markAuthCodeUsed` against a helper whose meaning had changed underneath it, which typechecked and passed lint while producing wrong user-facing error copy.
  - Two hash helpers with byte-identical bodies are not duplication worth collapsing when they hash different secrets: the reason to have one function per column is so a future change to one (e.g. an HMAC key) can't leak into the other.
  - When a story's `showboat` block pipes through `tail -N`, adding assertions to the underlying script shifts the window and fails `showboat verify` even though nothing is broken — bump the `-N` alongside the new assertions.
  - When multiple prior stories' PRs are open-but-unmerged and a later story depends on one of them, branch off that dependency's branch (not `main`) and note it in progress.txt — do not re-derive/duplicate its helpers. Confirm with `git branch -a`/`gh pr list` before picking a story: if the lowest-priority `passes:false` story already has an open PR from a previous iteration, skip it for this iteration and move to the next-lowest `passes:false` story that has no branch/PR in flight yet, rather than redoing already-completed work.
  - `getActiveAuthCode` (unused+unexpired only) and `getLatestAuthCode` (any state, newest row) serve different purposes — request-code-style "is there currently an active code" checks use the former; verify-code-style "what should I compare/reject against, and why" checks use the latter. Don't conflate them.
  - Use `timingSafeEqual` (after converting hex-encoded hashes to equal-length `Buffer`s) rather than `===`/`!==` for any secret-comparison logic (code hashes, token hashes, etc.) to avoid a timing side-channel — this repo's `hashesMatch` helper in `verify-code/+server.ts` is the reference pattern.
  - Standalone `tsx` verify scripts that need to build their own `drizzle(createClient(...))` instance outside a repo directory can't resolve `node_modules` unless run with the repo as cwd — a script placed in an external tmp dir will fail with `ERR_MODULE_NOT_FOUND` even with correct import paths; always run/co-locate such scripts from within the repo.
---

## [2026-07-27] - US-B04
- Note: branched off `ralph/us-b03-verify-code-session` (not merged to `main` yet) since this story depends on that branch's auth endpoints, same stacking reasoning as US-B02/US-B03.
- Rewrote `src/routes/login/+page.svelte`: a `request`/`code` two-step Svelte 5 runes state machine. `request` step shows a centered app mark + single "Send me a code" button (no email input anywhere). On click, `POST /api/auth/request-code`; on success transitions to the `code` step (auto-focused 6-digit monospace `inputmode="numeric"` field). Submitting calls `POST /api/auth/verify-code`; an incorrect code shows an inline `text-danger` error without leaving the page; a valid code calls `goto(resolve('/inbox'))`. 429 responses on either endpoint render "Too many ... Try again in N minute(s)." using the new `retryAfterMinutes` field.
- Added `getOldestAuthCodeRequestSince(database, windowStart)` to `src/lib/server/auth/auth-codes.ts` and wired it into `POST /api/auth/request-code`'s existing 429 branch to compute `retryAfterMinutes` (oldest in-window request's `createdAt` + the rate-limit window, minutes remaining until it ages out, floored at 1).
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (succeeds). Browser-verified via rodney against the dev server with `window.fetch` patched for `request-code`/`verify-code` (no real Resend account provisioned in this environment, same outstanding manual step as US-B02/US-B03 — the endpoints' real behavior was already verified via curl in those stories): initial render (button only, no email input), request step, code-entry step, inline error on incorrect code, and 429 "try again in N minutes" copy. Valid-code redirect to `/inbox` confirmed live (`rodney url` -> `/inbox`, app shell renders). Full proof in `docs/demos/US-B04.md` (showboat verify passes, exit 0).
- Files changed: src/routes/login/+page.svelte, src/lib/server/auth/auth-codes.ts, src/routes/api/auth/request-code/+server.ts, docs/demos/US-B04.md (new), agents/prd.json, agents/progress.txt.
- PR review (#14) follow-up:
  - Removed the dead `429` branch in `handleVerifyCode`. `/api/auth/verify-code` is not rate-limited — every rejection is a `400` — so the block could never run. Resolved the mismatch by deciding verify-code stays un-rate-limited rather than adding a second server-side bound: the two existing limits (5 attempts per code, 3 codes per 10 minutes) already cap brute force at 15 guesses per 10 minutes against a 10^6 space. The reasoning is now a comment where the branch used to be, so the next reader doesn't re-add it.
  - Non-digit input is stripped at the input (`oninput`) instead of relying on `pattern`, which never fired because `onsubmit` calls `preventDefault()` and fetches manually. This also normalizes the `123 456`/`123-456` shapes people paste out of an email client. Dropped the now-useless `pattern` attribute.
  - `errorMessage` clears on input, so a failure no longer sits on screen through the whole retype. The alert `<p>` is wrapped in `{#key errorNonce}` so an identical consecutive error recreates the node — a live region whose text is byte-identical is not re-announced, so two "Incorrect code." responses in a row were announced once. Verified in the browser: the second node is a different DOM object (`n2 === n1` is `false`).
  - "Start over" (which only reset local state, leaving the issued code live) is now "Request a new code", and returns to the request step with sub-copy: "Any code we already sent stays valid until you request a new one." The old label invited two clicks that would invalidate a working code and spend a rate-limit slot.
  - The app mark is now an `<h1>` instead of a `<span>`.
  - Auto-submits once six digits are present, which is what autofill delivers and what OTP screens do now.
  - Collapsed `countAuthCodeRequestsSince` + `getOldestAuthCodeRequestSince` into one `getAuthCodeRequestWindow` returning `{ count, oldestCreatedAt }` from a single aggregate — the throttled path costs one round trip instead of two, and the two numbers can't be read from different moments. The `429` now also carries a `Retry-After` header derived from the *same* oldest-request timestamp as `retryAfterMinutes`, so header and body can't disagree (verified: `retry-after: 354` alongside `retryAfterMinutes: 6`).
- **A real Resend API key is now provisioned** in the local `.env` (it was a placeholder through US-B02/B03). Clicking "Send me a code" during this story's browser verification delivered a real email to `AUTH_RECIPIENT_EMAIL`. US-B02's "outstanding manual step" is resolved for local dev; adding both vars to Vercel project settings is still pending.
- **Learnings for future iterations:**
  - `retryAfterMinutes` is computed from the *oldest* request in the rate-limit window (the one that will next age out), not the current time plus the full window — using the full window would overstate the wait whenever some of the window has already elapsed.
  - A dead client branch for a server response that can't occur is a signal worth chasing, not just dead code to delete — it usually means the UI and the server disagree about what protections exist. Resolve the disagreement (here: confirm the guess bound is 15/10min and write it down), then delete the branch.
  - `role="alert"` does not re-announce when the new text is byte-identical to the old. Wrap the live-region node in `{#key <nonce>}` and bump the nonce whenever an error is set, so repeated identical failures are each announced.
  - `pattern` on an input is inert when the form's `onsubmit` calls `preventDefault()` — native constraint validation never runs. Normalize in `oninput` instead.
  - Same branch-off-open-dependency pattern as US-B02/US-B03: since US-B01/US-B02/US-B03 (#11/#12/#13) were all still open/unmerged when this story started, this branch stacks on `ralph/us-b03-verify-code-session` rather than `main`.
---

## [2026-07-23] - US-J03
- Added three shared, dark/light-aware components in `src/lib/components/`, all built on the Dusk Terminal tokens from US-J01:
  - `EmptyState.svelte`: `message` (required) + optional `subCopy`, centered/quiet single-line copy, no illustration, per the design system's Empty rule.
  - `Skeleton.svelte`: `rows` (default 3), renders that many pulsing (`animate-pulse`) `bg-surface` rows for list-shaped content, per the design system's Loading rule (skeletons not spinners for list views).
  - `ErrorMessage.svelte`: `message` (required) + optional `onRetry` callback; renders inline in `text-danger`, with a bordered "Retry" button only when `onRetry` is passed, per the design system's Error rule.
- Wired all three into `src/routes/dev/tokens/+page.svelte` (existing unauthenticated dev-only QA page from US-J01/US-J02) under a new "Shared states" section, for visual QA in both themes. Not yet consumed by real features — that happens per-story in US-F01 (Inbox List), US-G01 (Thread View), US-H01 (Compose), US-I01 (Contacts) per prd-ui-ux.md's FR-4.
- Verified: `npm run check` (0 errors), `npm run lint` (clean — had to fix an initial `@typescript-eslint/no-unused-vars` error in `Skeleton.svelte` by deriving an index array with `Array.from` instead of an `{#each {length: rows} as _}` placeholder-variable form), `npm run build` succeeds. Verified live via rodney against `/dev/tokens`: EmptyState renders message+sub-copy; Skeleton renders 3 pulsing rows; ErrorMessage renders in `rgb(201, 127, 127)` = `#c97f7f` (matches `--dt-danger`) with a working Retry button (click incremented a counter bound to `onRetry`). Caught and fixed a demo-only bug where the Skeleton's demo wrapper used `bg-surface` same as the skeleton rows themselves, making them invisible — changed the wrapper to `bg-background` (the component itself was already correct per spec). Confirmed both dark and light themes via the existing `ThemeToggle`. Screenshots and full proof recorded in docs/demos/US-J03.md (showboat verify passes, exit 0).
- Files changed: src/lib/components/EmptyState.svelte (new), src/lib/components/Skeleton.svelte (new), src/lib/components/ErrorMessage.svelte (new), src/routes/dev/tokens/+page.svelte, docs/demos/US-J03.md (new, + 2 screenshot assets), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - When a "surface"-toned component (e.g. this Skeleton) is demoed inside another surface-toned container, the two blend together and visually disappear — demo/QA wrappers for surface-tone components need a `bg-background` (or otherwise contrasting) container, not `bg-surface`, even though real usage in an actual list will naturally have that contrast from surrounding page chrome.
  - Svelte 5 + this ESLint config flags `{#each {length: n} as _, i (i)}`-style unused destructured placeholders as `@typescript-eslint/no-unused-vars`; prefer `$derived(Array.from({ length: n }, (_, i) => i))` and `{#each indices as i (i)}` to iterate a fixed count without an unused loop variable.
  - `src/lib/components/` (created in this story) is now the home for shared, feature-agnostic UI components (EmptyState/Skeleton/ErrorMessage/ThemeToggle) — future shared components (not tied to one feature) should live there too, distinct from feature-specific components that will live under their own route/feature folders.
---

## [2026-07-27] - US-B05
- Replaced US-A03's placeholder route protection with real server-side session validation, and moved logout to `POST /api/auth/logout` where it deletes the `sessions` row rather than only clearing the cookie.
- `src/lib/server/auth/session.ts`: dropped `hasSessionCookie` (cookie-presence only). The module now owns the session *cookie* end to end — `SESSION_COOKIE_NAME`, `SESSION_TTL_MS` (30 days, FR-9), `setSessionCookie`/`clearSessionCookie`, `validateSession(db, cookies, now?)`, `destroySession(db, cookies)` — as the counterpart to `sessions-store.ts`, which owns the *table*. It takes the db handle as a first argument and imports `Cookies` as a type only, so it keeps the no-`$env` property that lets the standalone `tsx` verify script exercise it.
- `validateSession` hashes the cookie token with `hashSessionToken`, looks it up via `getValidSessionByTokenHash`, and returns `null` for missing/unknown/expired alike — distinguishing them would leak whether a given token ever existed, and route protection treats all three identically. FR-9 sliding expiration refreshes `expires_at` only once the session is past the halfway point of its TTL (and re-sets the cookie when it does), so it costs roughly one write per 15 days instead of one per page view while still meaning anyone active within any 15-day span never expires.
- `src/routes/(app)/+layout.server.ts`: calls `validateSession(db, cookies)`, and clears the cookie before redirecting so a stale/expired token isn't re-sent on every subsequent request.
- `src/routes/api/auth/logout/+server.ts` (new, replaces `src/routes/logout/+server.ts`): `destroySession` then `303` to `/login` — 303 so the browser follows with a GET, since the app shell's logout control is a plain JS-free `<form method="POST">`. Deleting the row is the FR-8 property; clearing only the cookie would leave a live row a copied token could still use.
- `src/routes/(app)/+layout.svelte`: the existing top-bar "Log out" form's action moved to `/api/auth/logout` (the button itself was already there from US-J02, satisfying the "visible logout action" criterion).
- `src/routes/api/auth/verify-code/+server.ts`: uses the shared `setSessionCookie`/`SESSION_TTL_MS` instead of its own inline `cookies.set` options and TTL constant.
- Extended `src/lib/server/auth/verify-auth-sessions.mts` with 11 new assertions for `session.ts` (30 total, all passing against the live Turso DB): accept a live token, don't refresh a fresh session, slide `expires_at` past the halfway point and re-set the cookie, reject unknown/absent/expired, and both `destroySession` outcomes. Added a `fakeCookies` stand-in for SvelteKit's `Cookies` and a tracked-hash cleanup list so the `finally` block removes every seeded session row.
- Verified: `npm run check` (0 errors), `npm run lint` (clean), `npm run build` (succeeds). Over HTTP, `/inbox` 302s to `/login` both with no cookie and with a non-empty bogus cookie — that second case returned **200** before this story, and is the actual regression fixed. Browser-verified with rodney via a real login (stubbed `request-code` only, real `verify-code`, a known code seeded into `auth_codes`): reached `/inbox` (sessions rows 0 -> 1), clicked "Log out", landed on `/login` with sessions rows 1 -> 0 and no `session` cookie, and a fresh `/inbox` visit redirected again. Full proof in `docs/demos/US-B05.md` (showboat verify exits 0).
- Files changed: src/lib/server/auth/session.ts, src/lib/server/auth/verify-auth-sessions.mts, src/routes/(app)/+layout.server.ts, src/routes/(app)/+layout.svelte, src/routes/api/auth/logout/+server.ts (new), src/routes/logout/+server.ts (deleted), src/routes/api/auth/verify-code/+server.ts, CLAUDE.md, docs/demos/US-B05.md (new, + 2 screenshots), agents/prd.json, agents/progress.txt.
- **Learnings for future iterations:**
  - `cookies.delete(name, options)` must be given the same `path` the cookie was set with, or it silently doesn't delete. Define the cookie's name and flags once in a module and route every set/delete through helpers — two call sites specifying options independently is a bug that only shows up as "logout didn't work".
  - `curl -D-` writes response headers with CRLF line endings, so a showboat `exec` block that captures them records a trailing `\r` and `showboat verify` then fails with an expected/actual diff that looks byte-identical on screen. Pipe header captures through `tr -d '\r'`. (Generalizes the existing "strip nondeterminism" rule: also strip *invisible* bytes.)
  - `grep -c error` is a bad build-success check in this project — SvelteKit's build output lists `error.svelte.js` (the fallback error page chunk) on every successful build. Use `npm run build >/dev/null && echo success` instead.
  - A standalone `tsx` verify script can exercise SvelteKit-request-scoped code (anything taking `Cookies`) with a small fake that records `set`/`delete` calls, as long as the module under test imports `Cookies` as a *type*. This is what makes time-dependent logic like sliding expiration assertable — pass an explicit `now` instead of waiting out a real TTL.
  - Such a script must live inside the repo: one placed in an external tmp dir fails with `ERR_MODULE_NOT_FOUND` on `@libsql/client` even when run with the repo as cwd (module resolution is relative to the script's own path, not cwd).
  - To browser-test an authenticated page, don't try to set the session cookie directly (rodney has no cookie command) — seed a known code hash into `auth_codes` with `hashAuthCode`, stub only `/api/auth/request-code` in-page, and let the real `verify-code` mint the session. That also means route protection is being exercised against a genuinely-issued cookie rather than a hand-made one.
  - The login page's "Send me a code" is `type="button"`, not `type="submit"` — a rodney `click "button[type=submit]"` selector will time out on `/login`.
---

## [2026-07-27] - US-E01
- Added the Svix signature gate in front of inbound email ingestion, plus the `svix` dependency.
- `src/lib/server/webhooks/svix.ts` (new): `verifySvixRequest(request)` reads the three Svix headers, reads the body as raw text, verifies with a memoized `Webhook` built from `RESEND_INBOUND_WEBHOOK_SECRET`, and returns a discriminated `{ ok: true, payload, rawBody } | { ok: false, reason }`.
- `src/routes/api/webhooks/resend-inbound/+server.ts` (new): `POST` handler. On failure it `console.warn`s the reason and returns a bare `401`; on success it currently just returns `{ ok: true }` — US-E02 hangs parsing/persistence off `result.payload` there.
- `src/lib/server/webhooks/verify-inbound-webhook.mts` (new): standalone HTTP smoke test driving the real endpoint with five request shapes, using `svix`'s own `sign()` for the positive case.
- `.env.example`: documented `RESEND_INBOUND_WEBHOOK_SECRET`. A throwaway generated `whsec_` value was added to the local gitignored `.env` so the endpoint is testable before the real dashboard secret exists.
- Verified: 5/5 checks pass against a running dev server (valid signature 200; tampered body + genuine signature 401; signature from a different secret 401; no svix headers 401; missing svix-id 401). `npm run check` 0 errors, `npm run lint` clean, `npm run build` succeeds. docs/demos/US-E01.md, `showboat verify` exits 0.
- **Learnings for future iterations:**
  - A webhook signature covers the *raw request bytes*. Never `request.json()` a signed request before verifying — a `Request` body can only be consumed once, and a re-serialized parse will not match the signature. Read text, verify, then parse the text (or use the verified payload the library returns).
  - Returning a *discriminated* verification result (payload present only on the success branch) rather than `{ verified: boolean, payload }` makes "persisted an unverified body" a type error instead of a code-review question. Worth reusing for any verify-then-act boundary.
  - Failure detail from an auth/signature check belongs in the server log, not the response. The client gets a bare status; a reason string is a forging aid.
  - The lazy-env-read pattern established for `email/resend.ts` applies to *any* server module reachable from a route: `npm run build` imports every `+server.ts` for HTTP-method detection, so import-time secret checks break the build/CI, not just dev.
  - For a showboat block that needs a running server plus a secret, make it self-contained: export a throwaway secret inline, start the dev server with it in the environment, `trap`-kill on exit. This keeps `showboat verify` reproducible without depending on the gitignored `.env`.
---

## [2026-07-27] - US-E02
- Turned the US-E01 signature gate into a real ingestion entry point: a verified `email.received` event is now parsed and its sender upserted into `contacts`.
- **Checked the payload shape against the real Resend account before writing any parser** (US-E01 explicitly flagged its fixture as a placeholder), and the PRD's assumption was wrong: the `email.received` webhook carries **metadata only** — no body, no headers, no attachment content. So `html`/`text`, the `From:` display name, `In-Reply-To`, `References` and the `Date:` header are only reachable via `resend.emails.receiving.get(email_id)`. Ingestion is inherently two calls. Updated `tasks/prd-feature-inbound-processing.md`, `tasks/prd-architecture.md` and US-E05's acceptance criteria in `agents/prd.json` accordingly (attachment content comes from the Attachments API, not base64-inline in the request body).
- `src/lib/server/inbound/parse.ts` (new): `parseInboundWebhookEvent(payload)` validates the envelope and returns a discriminated `{ ok: true, emailId } | { ok: false, reason }`; `parseReceivedEmail(record)` projects a fetched record onto `ParsedInboundEmail` (from/to/cc/bcc/reply-to, subject, html, text, receivedAt, messageId, inReplyTo, references, attachment metadata). Both pure — no env, db or network — so fixtures can drive them from a `tsx` script. Handles two real-world quirks: Resend JSON-encodes some header values (`date: "\"2026-07-25T15:15:31.000Z\""`), and the display name lives in `headers.from` while the trustworthy bare address is the record's own `from` field (the header is never read for the address).
- `src/lib/server/db/contacts.ts` (new): `normalizeEmail`, `getContactByEmail`, `upsertContactFromInbound`, db-handle-first like the auth helpers. Lowercases on write (what makes the case-sensitive `contacts_email_unique` index actually enforce one row per address) and compares with `lower()` on read; `onConflictDoNothing()` + re-read so a concurrent redelivery is a no-op; never renames an `auto_created = false` (hand-edited) contact.
- `src/lib/server/email/resend.ts`: added `fetchReceivedEmail(emailId)` alongside `sendAuthCodeEmail` — one Resend client, same lazy env read, same "log the provider's message, throw a generic Error" shape.
- `src/routes/api/webhooks/resend-inbound/+server.ts`: verify -> parse envelope -> fetch -> parse record -> upsert contact. A verified-but-unusable payload returns `200` (nothing to retry); a transient fetch/DB failure throws -> `500` (which Resend retries).
- Verified: 42 checks in `src/lib/server/inbound/verify-inbound-parse.mts` (new — fixtures including a redacted real delivery, plus the contact upsert against the live Turso DB with `finally` cleanup), and `src/lib/server/webhooks/verify-inbound-webhook.mts` extended from 5 to 11 checks: the five signature cases plus a genuinely-signed envelope naming a **real** received email, which the endpoint fetches and ingests (`200`, sender in `contacts` with the display name `Google` pulled from the real `headers.from`), a redelivery that does not duplicate the contact, and a non-received event type ignored with `200`. `npm run check` 0 errors, `npm run lint` clean, `npm run build` succeeds. `docs/demos/US-E02.md`, `showboat verify` exits 0.
- Files changed: src/lib/server/inbound/parse.ts (new), src/lib/server/inbound/verify-inbound-parse.mts (new), src/lib/server/db/contacts.ts (new), src/lib/server/email/resend.ts, src/lib/server/webhooks/verify-inbound-webhook.mts, src/routes/api/webhooks/resend-inbound/+server.ts, CLAUDE.md, docs/demos/US-E02.md (new), agents/prd.json, agents/progress.txt, tasks/prd-feature-inbound-processing.md, tasks/prd-architecture.md.
- **Learnings for future iterations:**
  - A webhook payload is a claim *about* a resource, not the resource. Fetch one real payload from the provider before writing a parser — the provider's list/get API usually makes this possible without waiting on a live delivery (`resend.emails.receiving.list()` had real inbound emails sitting there).
  - Header values from a provider may arrive JSON-encoded and lowercase-keyed. Decode defensively (only when the decode yields a string) so a legitimately-quoted value like `"Google" <a@b>` isn't mangled.
  - Read the bare address from the field the provider validated, and the display name from the header. Trusting the header for both lets a crafted `From:` disagree with the envelope.
  - Webhook status codes are retry instructions: `200` for verified-but-unusable, `5xx` only for transient. `console.warn` the reason either way.
  - This story's real end-to-end run created a genuine `contacts` row (`no-reply@accounts.google.com`) in the live DB from an actual inbound email — it was left in place rather than cleaned up, since it is real data the app should have. Expect it when US-F01/US-I01 first render lists.
---
